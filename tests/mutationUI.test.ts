import { describe, it, expect } from 'vitest';

describe('DOM mutation mechanism.', () => {
	it('will initialize the DOM mutation mechanism.', () => {
		// const mutation = new Observer().whenMutations(() => {});
		expect('').toBeDefined();
	});
});
