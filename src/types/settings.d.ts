type Settings = {
	autofocus?: boolean;
	content?: Object | HTMLElement;
	debounce?: number;
	editable?: boolean;
	extensions?: Array<any>;
	holder?: HTMLElement | null;
	placeholder?: string;
};

export default Settings;
